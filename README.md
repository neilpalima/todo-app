# todo-app

## How to run the application

- First, go inside the backend folder and run the server
- Then, go inside the frontend folder and run application

Note: Do not forget to run `npm i` in both folders first before running the server and the application